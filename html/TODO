professor_page.php:
    -generate professor_tests view based on username from $_SESSION variable. This should be query on tests -> students -> courses -> professors.email
    
proctors.php:
    -display proctor's weekly schedule based on username from $_SESSION variable. Direct query from proctors -> proctors_schedule
    
runLogin.php:
    -remove proctor/professor page links LAST